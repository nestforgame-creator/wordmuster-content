name: Validate Story Data

on:
  pull_request:
    paths:
      - "data/story/**"
      - "scripts/validate/**"
      - "package.json"
      - "package-lock.json"

  push:
    branches: [main, content-improvement-pass-1]
    paths:
      - 'data/story/**'
      - 'scripts/validate/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/validate-story.yml'

  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate story data
        run: node scripts/validate/validate-story-data.js

      - name: Validate story words
        run: node scripts/validate/validate-story-words.js
